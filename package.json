{
  "name": "rf-archive-guide",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "serve .",
    "build": "mkdir -p dist && cp -r * dist/"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
